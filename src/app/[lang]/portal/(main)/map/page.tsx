// src/app/portal/(main)/map/page.tsx
'use client';

/**
 * This page is intentionally empty.
 * The (main)/layout.tsx detects the /portal/map pathname
 * and injects <MapView /> for us, so here we can just
 * return null (or an empty fragment).
 */
export default function MapRouteStub() {
  return null;
}
